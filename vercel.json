{
    "version": 2,
    "name": "Paradise_Films_backend",
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "methods": ["GET", "POST", "PUT", "DELETE"],
            "headers": ["Authorization"]
        }
    ]
}